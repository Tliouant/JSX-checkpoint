import React from 'react';

const FullName = () => {
  const style = {
    fontSize: "24px",
    fontWeight: "bold",
    color: "#333"
  };

  return (
    <div style={style}>Tliouant Mohamed el amine</div>
  );
};

export default FullName;